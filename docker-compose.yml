services:
  capstone-web:
    build:
      context: .
      dockerfile: Dockerfile
    image: capstone-portal:latest
    container_name: capstone_portal_server
    ports:
      - "3000:80"
    volumes:
      # Mount frontend directory for live sync during development testing
      - ./frontend:/usr/share/nginx/html:ro
    restart: unless-stopped
